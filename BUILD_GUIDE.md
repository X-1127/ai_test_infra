# LLM Mock Server 打包指南

本指南说明如何将 LLM Mock Server 项目打包成独立的可执行文件，包括后端服务器和桌面应用。

## 📦 打包说明

### 前提条件

- Python 3.13 或更高版本
- Windows 操作系统（打包目标）
- 管理员权限（用于安装依赖）

### 快速开始

1. **运行打包脚本**

   在项目根目录下运行：
   ```bash
   build.bat
   ```

2. **等待打包完成**

   打包过程可能需要 5-10 分钟，具体时间取决于您的系统性能。

3. **获取可执行文件**

   打包完成后，可执行文件位于：
   ```
   dist\LLM_Mock_Server.exe
   ```

## 🚀 分发说明

### 单文件分发

打包后的 `LLM_Mock_Server.exe` 是一个独立的可执行文件，包含了所有必要的依赖和资源。

**分发步骤：**

1. 将 `dist\LLM_Mock_Server.exe` 复制到目标机器
2. 双击运行即可
3. 无需安装 Python 或其他依赖

### 首次运行

首次运行时，程序会自动：
- 创建必要的配置目录
- 生成默认配置文件
- 启动桌面应用界面

## 📋 打包配置

### build.spec 文件

打包配置文件 `build.spec` 包含以下设置：

- **入口点**: `desktop/main.py`（桌面应用入口）
- **包含内容**:
  - 后端服务器代码（`app/`）
  - 桌面应用代码（`desktop/`）
  - 配置文件示例（`config/responses.yaml.example`）
  - 服务器启动脚本（`scripts/start_server.py`）
- **隐藏导入**: 所有必要的 Python 模块
- **排除模块**: 不需要的第三方库（如 numpy、matplotlib 等）
- **窗口模式**: 无控制台窗口（GUI 应用）
- **UPX 压缩**: 启用以减小文件大小

### pyproject.toml 配置

项目配置文件包含：

- **项目依赖**: 后端和桌面应用所需的所有依赖
- **打包依赖**: PyInstaller 及其相关工具
- **脚本入口**: 定义了命令行和 GUI 入口点
- **PyInstaller 配置**: 默认打包参数

## 🔧 自定义打包

### 修改应用图标

1. 准备一个 `.ico` 格式的图标文件
2. 将其放置在 `assets/icon.ico`
3. 重新运行 `build.bat`

### 修改应用名称

编辑 `build.spec` 文件中的 `name` 参数：
```python
exe = EXE(
    ...
    name='YourAppName',  # 修改这里
    ...
)
```

### 添加额外资源文件

在 `build.spec` 的 `datas` 列表中添加：
```python
datas = [
    (str(project_root / 'config' / 'responses.yaml.example'), 'config'),
    (str(project_root / 'your_resource.txt'), 'resources'),  # 添加这行
]
```

## 🐛 常见问题

### 打包失败

**问题**: 打包过程中出现错误

**解决方案**:
1. 确保已安装所有依赖：`pip install -e ".[desktop,build]"`
2. 清理旧的构建文件：删除 `build/` 和 `dist/` 目录
3. 检查 Python 版本是否为 3.13+
4. 查看错误日志，根据错误信息调整 `build.spec`

### 运行时错误

**问题**: 可执行文件运行时出现错误

**解决方案**:
1. 临时启用控制台模式查看错误信息
2. 在 `build.spec` 中设置 `console=True`
3. 重新打包并运行，查看控制台输出
4. 根据错误信息添加缺失的隐藏导入

### 文件过大

**问题**: 可执行文件太大（超过 100MB）

**解决方案**:
1. 确保 UPX 压缩已启用（默认已启用）
2. 排除更多不需要的模块
3. 使用 `--onefile` 模式（默认已使用）

### 缺少配置文件

**问题**: 运行时提示找不到配置文件

**解决方案**:
1. 确保在 `build.spec` 中正确添加了配置文件
2. 检查文件路径是否正确
3. 重新打包

## 📊 打包输出

### 目录结构

打包完成后，会生成以下目录结构：

```
llm-mock-server/
├── build/              # 构建临时文件
├── dist/               # 最终可执行文件
│   └── LLM_Mock_Server.exe
└── build.spec          # 打包配置文件
```

### 文件大小

- **未压缩**: 约 80-120 MB
- **UPX 压缩后**: 约 40-60 MB

## 🎯 验证打包结果

### 测试可执行文件

1. 双击 `dist\LLM_Mock_Server.exe`
2. 检查桌面应用是否正常启动
3. 测试服务器启动/停止功能
4. 测试配置管理功能
5. 测试日志查看功能
6. 测试测试界面功能

### 功能检查清单

- [ ] 桌面应用启动
- [ ] 服务器启动
- [ ] 服务器停止
- [ ] 延迟注入配置
- [ ] 故障注入配置
- [ ] YAML 配置管理
- [ ] 日志查看
- [ ] 测试界面
- [ ] 性能监控

## 📝 版本发布

### 发布流程

1. 更新版本号（`pyproject.toml` 中的 `version`）
2. 运行打包脚本
3. 测试可执行文件
4. 创建发布说明
5. 分发可执行文件

### 版本命名

遵循语义化版本控制：`主版本号.次版本号.修订号`

- **主版本号**: 不兼容的 API 变更
- **次版本号**: 向下兼容的功能新增
- **修订号**: 向下兼容的问题修正

## 🔗 相关资源

- [PyInstaller 官方文档](https://pyinstaller.org/)
- [PyQt6 官方文档](https://www.riverbankcomputing.com/static/Docs/PyQt6/)
- [FastAPI 官方文档](https://fastapi.tiangolo.com/)

## 💡 提示

- 首次打包建议在虚拟环境中进行
- 打包前确保所有测试通过
- 定期更新依赖包以获得最佳性能
- 保留打包配置文件的版本控制